(async()=>{const host=document.querySelector("#study-text-list");if(!host)return;const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));try{const data=await (await fetch("../../js/data/studies-data.json",{cache:"no-store"})).json();const items=(data.texts||[]).filter(x=>x.published!==false);if(!items.length){host.innerHTML='<div class="study-placeholder"><h2>No texts yet</h2><p>Published PDFs and Word documents will appear here.</p></div>';return}host.innerHTML=items.map(x=>`<article class="study-text-entry"><div>${x.category?`<p class="eyebrow">${esc(x.category)}</p>`:""}<h2>${esc(x.title)}</h2><p>${esc(x.description||"")}</p></div><a class="outline-button" href="../../${encodeURI(x.path)}" target="_blank" rel="noopener">${x.mime==="application/pdf"?"Open PDF":"Open / Download"}</a></article>`).join("")}catch(e){host.innerHTML='<div class="study-placeholder"><p>Could not load the texts library.</p></div>'}})();